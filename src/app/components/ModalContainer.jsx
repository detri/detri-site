import styled from 'styled-components';

const ModalContainer = styled.div`
  border-radius: 0.5em;
  height: 75%;
  width: 75%;
  left: 0;
  top: calc(0 - 100%);
  z-index: 2;
`;

export default ModalContainer;

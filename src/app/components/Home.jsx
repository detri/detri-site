import React from 'react';
import styled from 'styled-components';

const StyledTitle = styled.h1`
  text-align: center;
`;

const Home = () => {
  return (
    <StyledTitle>
      What's up, nerd?
    </StyledTitle>
  );
};

export default Home;
